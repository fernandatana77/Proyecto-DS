{
  "name": "sistema-trazabilidad-inventario-tic",
  "version": "1.0.0",
  "description": "Sistema de Trazabilidad e Inventario TIC Multi-Sede (API REST + PWA offline-first)",
  "main": "servidor.js",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env servidor.js",
    "dev": "node --env-file-if-exists=.env --watch servidor.js",
    "seed": "node --env-file-if-exists=.env scripts/seed.js",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "keywords": [
    "inventario",
    "tic",
    "prestamos",
    "auditoria",
    "pwa",
    "postgresql"
  ],
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  }
}